%%% This file is generated; DO NOT EDIT MANUALLY.

-module(<%= @basename %>_SUITE).
-compile(export_all).
-include("ct.hrl").

-define(print_error_or_return(Res, Testcase),
        case Res of
            true ->
                {test, ok};
            false ->
                io:format("EXPECTED:~n    ~p~nGOT:~n    ~p~nCONV:~n    ~p~n~n",
                          [E, G2, test_util:conv_from_get(G2)]),
                exit("FAIL: Mismatch in " ++ atom_to_list(Testcase) ++
	             " <%= @basename %>_SUITE"	)
        end).

-define(test(Func, Path, Ref, Expected),
        Func(_Config) ->
                E = Expected,
                G = test_util:hnget("/" ++ "<%= @basename %>" ++ Path, Ref),
               {struct,Props} = mochijson:decode(G),
               G2 = proplists:get_value("rawvalue", Props),
               Res = test_util:cmp(G2, E),
               ?print_error_or_return(Res, Func)).

%%% TESTS ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
<%= @testcasedata.map { |rec| "?test(#{rec[0]}, \"#{rec[1]}\", \"#{rec[2]}\", #{rec[3]}).\n" } %>
%%% ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

init_per_suite(Config) ->
    test_util:import_xls("<%= @basename %>"),
    Config.

end_per_suite(_Config) ->
    ok.

init_per_testcase(_TestCase, Config) ->
    Config.

end_per_testcase(_TestCase, _Config) ->
    ok.

test(_Config) ->
    ok.

all() ->
    [
      <%= @testcasedata.map { |rec| rec[0] }.join(",\n        ") %>
    ].
