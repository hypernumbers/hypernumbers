%%% Lexer for <%= @lang %> frontend for Muin.
%%% Hasan Veldstra <hasan@hypernumbers.com>
%%% Generated by generate_lexer.rb on <%= Time.now %>

Definitions.

STRING    = (\"[^"\n]*\")
%%" % Syntax highlighting fix.
WHITESPACE = ([\000-\s]*)
ALLOWED_PREFIXES = (\s+|=|\+|\-|\*|\/)

<%= @function_defs %>


Rules.

%% Preserve strings as-is.
{STRING} : {token, {string, YYtext}}.
%% Kill whitespace.
{WHITESPACE} : .

<%= @function_rules %>

\n : {end_token, {'$end'}}.
.  : {token, {stuff, YYtext}}.

